var expect = require("expect.js")

describe('someone', function() {
  describe('that do something', function() {
    it('should end up with something', function() {
      expect(true).to.be(true)
    })
  })
})
